// import { hasFullAuthorities, Authority, hasAuthorities, authorityMap } from '@/common/authority'

// export default function authority (value: number) {
//   value = +value
//   if (hasFullAuthorities(value)) return '完全控制'
//   let ret: string[] = []
//   Object.keys(authorityMap).forEach((key: any) => {
//     if (hasAuthorities(value, key)) ret.push(authorityMap[key])
//   })
//   return ret.join(',')
// }
